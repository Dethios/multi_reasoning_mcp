{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run MCP server",
      "type": "python",
      "request": "launch",
      "module": "multi_reasoning_mcp.server",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}
